# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/

# React
- When implementing scroll-on-pagination, use scrollIntoView on a section ref instead of window.scrollTo to scroll to the section container (where the first card/item is), not the absolute page top. Confidence: 0.65

